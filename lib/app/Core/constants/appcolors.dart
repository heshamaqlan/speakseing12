import 'package:flutter/material.dart';

class AppColors {
  // Base Colors
  static const Color base = Color(0xff8B3DFF);

  // Backgrounds
  static const Color background = Color.fromARGB(255, 245, 244, 246);

  // Text Colors
  static const Color textBlack = Color(0xff333333);
  static const Color textWhite = Colors.white;
  static const Color grey = Color.fromARGB(255, 88, 87, 87);
}
